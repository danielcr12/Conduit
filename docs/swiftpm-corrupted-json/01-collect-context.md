Prompt:
You are a Swift 6.2 engineer. Gather complete error context and reproducible details for the SwiftPM corrupted JSON failure.

Goal:
Capture precise error text, triggering command, environment details, and recent changes to enable reliable reproduction.

Task BreakDown:
- Record the exact error message and stack trace (if any).
- Note the command that triggers the error and when it occurs.
- List recent changes (dependencies, tools, build settings).
- Record repo path and affected package(s).
- Capture SwiftPM and Swift versions via `swift --version`.
- Record OS version and whether Xcode or CLI SwiftPM is used.
